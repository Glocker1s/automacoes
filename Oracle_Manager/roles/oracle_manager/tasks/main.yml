---
- name: Definir fase efetiva da role
  ansible.builtin.set_fact:
    orm_effective_phase: "{{ orm_phase | default('controller') | string | lower }}"

- name: Validar fase solicitada
  ansible.builtin.assert:
    that:
      - orm_effective_phase in ['controller', 'backup', 'restore', 'report']
    fail_msg: >-
      Fase inválida para oracle_manager: {{ orm_effective_phase }}.
      Valores aceitos: controller, backup, restore, report.

- name: Executar controller
  ansible.builtin.include_tasks: controller.yml
  when: orm_effective_phase == 'controller'

- name: Executar backup
  ansible.builtin.include_tasks: backup.yml
  when: orm_effective_phase == 'backup'

- name: Executar restore
  ansible.builtin.include_tasks: restore.yml
  when: orm_effective_phase == 'restore'

- name: Executar report
  ansible.builtin.include_tasks: report.yml
  when: orm_effective_phase == 'report'
